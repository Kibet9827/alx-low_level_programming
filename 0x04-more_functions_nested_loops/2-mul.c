#include "main.h"

/**
 * mul - multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply to the first number
 * Return: The result of the multiplication of x param and y param
 */

int mul(int x, int y)
{
	return (x * y);
}
